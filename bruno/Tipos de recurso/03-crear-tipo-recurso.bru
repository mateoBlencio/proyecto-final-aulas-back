meta {
  name: Crear tipo de recurso
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/v1/resource-types
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Pizarrón digital",
    "valueKind": "BOOLEAN"
  }
}

docs {
  POST /v1/resource-types. Rol: SUBSECRETARIA.
  name: obligatorio, máx. 100, único (incluye desactivados) => 400 si se repite.
  valueKind: COUNT (cantidad real) o BOOLEAN (0/1).
}
