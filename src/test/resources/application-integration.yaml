spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///siga?TC_DAEMON=true
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

  jpa:
    hibernate:
      ddl-auto: create-drop

  docker:
    compose:
      enabled: false

siga:
  solver:
    unimproved-seconds-limit: 1
